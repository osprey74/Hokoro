# =====================================================================
#  Wokwi simulator config for Hokoro
#  Build first: pio run -e sim
#  Then in VS Code: F1 -> "Wokwi: Start Simulator"
# =====================================================================
[wokwi]
version = 1
firmware = '.pio/build/sim/firmware.bin'
elf = '.pio/build/sim/firmware.elf'
