{
  "name": "hokoro-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "kv:create": "wrangler kv namespace create SCHEDULE",
    "kv:list": "wrangler kv key list --binding=SCHEDULE",
    "kv:get": "wrangler kv key get --binding=SCHEDULE events"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
